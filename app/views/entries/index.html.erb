<h2><%= @journal.title.capitalize %></h2>

<%= simple_form_for [@journal, @entry] do |m| %>
  <%= m.input :goal  %>
  <%= m.simple_fields_for :milestones do |p| %>
    <%= p.input :title, label: "Add 1, 2, or 3 milestones!", :class => "form-textfield" %>
  <% end %>
  <%= m.simple_fields_for :milestones do |p| %>
    <%= p.input :title, label: false, :class => "form-textfield" %>
  <% end %>
    <%= m.simple_fields_for :milestones do |p| %>
    <%= p.input :title, label: false, :class => "form-textfield" %>
  <% end %>
  <%= m.submit "Create", :class => "form-submit"%>
<% end %>

<div class="icon-div">
<i class="fas fa-arrow-down"></i>
</div>



<% @entries.each do |entry| %>
  <button class="collapsible"><%= entry.goal %></button>
  <div class="content">
    <%=  link_to entry.goal.capitalize, entry_path(entry), :class => "link-title" %> Milestones:
    <% entry.last_three_milestones.each do |milestone| %>
      <%= simple_form_for([milestone, Pomodoro.new]) do |m| %>
          <!-- <i class="far fa-clock"></i> -->
        <%= button_tag do %>
          <p><i class="far fa-clock"></i><%= milestone.title %></p>
        <% end %>
      <% end %>
    <% end %>
  </div>
<% end %>

